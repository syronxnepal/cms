import themeVars from 'assets/scss/_theme-vars.module.scss';

const customShadows = {
  low: themeVars.shadowLow,
  medium: themeVars.shadowMedium,
  high: themeVars.shadowHigh,
  border: themeVars.shadowBorder,
};

export default customShadows;
